apiVersion: v1
kind: ServiceAccount
metadata:
  name: lecture
  namespace: lecture
imagePullSecrets:
  # Ensure this Secret exists in the same namespace (create or mirror via reflector).
  - name: harbor-auth
