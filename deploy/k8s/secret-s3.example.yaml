# Template only. Do NOT apply as-is.
#
# Create a Secret in the `lecture` namespace with these keys:
# - accessKeyId
# - secretAccessKey
#
# Recommended: encrypt before committing (SOPS + age), e.g.:
#   /mnt/data/Documents/agent-cluster/agentcluster-Flux/encrypt-secret.sh lecture/deploy/k8s/secret-s3.secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: lecture-s3
  namespace: lecture
type: Opaque
stringData:
  accessKeyId: "REPLACE_ME"
  secretAccessKey: "REPLACE_ME"
